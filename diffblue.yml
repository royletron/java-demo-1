buildCmd: mvn -e -X -Dmaven.test.skip=true compile; 
testCmd: mvn test
classpathCmd: cd ../results/; grep -o -P '\\-classpath .*? ' build_log.txt | awk '{print $2}'
cbmcArguments:
  java-max-input-array-length: 10 
  unwind: 10
  depth: 10 
  java-max-vla-length: 10
  string-max-length: 10 
